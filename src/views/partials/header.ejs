<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Literata:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="theme-dark <%= title === 'Book Notes App' ? 'home-page' : 'app-page' %>">
    <header>
        <div class="logo-container" onclick="location.href='/'">
            <!-- The SVG Scribble Background -->
            <svg class="scribble-svg" viewBox="0 0 300 100" preserveAspectRatio="none">
                <!-- A chaotic, energetic scribble path -->
                <path class="path" d="M20,50 C40,20 60,80 90,40 S140,70 160,30 S220,80 250,40 S290,60 280,50" />
            </svg>

            <div class="logo-text">SCR<span class="i-dot">I</span>BL</div>
        </div>
        <nav>
            <ul>
                <% if (user) { %>
                    <li><span>Welcome, <%= user.username %></span></li>
                    <li><a href="/books">My Books</a></li>
                    <li><a href="/search">Search</a></li>
                    <li><a href="/users">Users</a></li>
                    <li><a href="/feed">Feed</a></li>
                    <li><a href="/profile">Profile</a></li>
                    <li><a href="/logout">Logout</a></li>
                <% } else { %>
                    <li><a href="/login">Login</a></li>
                    <li><a href="/register">Register</a></li>
                <% } %>
            </ul>
        </nav>
    </header>
    <main>